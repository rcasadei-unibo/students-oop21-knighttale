name: Spotbugs

on:
  workflow_call:

jobs:
  spotbugs:
    runs-on: ubuntu-latest
    steps:
      - uses: jwgmeligmeyling/spotbugs-github-action@master
        with:
          path: '**/spotbugs/excludes.xml'